# IBM DB2i MCP Server Configuration
# Copy this file to .env and fill in your values

# Required: IBM i hostname or IP address
DB2I_HOSTNAME=your-ibm-i-host.com

# Optional: Port number (default: 446)
DB2I_PORT=446

# Required: IBM i username
DB2I_USERNAME=your-username

# Required: IBM i password
DB2I_PASSWORD=your-password

# Optional: Database name (default: *LOCAL)
DB2I_DATABASE=*LOCAL

# Optional: Default schema/library
DB2I_SCHEMA=

# Optional: Additional JDBC options (semicolon-separated)
# Example: naming=sql;date format=iso;time format=iso
DB2I_JDBC_OPTIONS=naming=sql;date format=iso
